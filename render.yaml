services:
  - type: web
    name: modex-background-remover
    runtime: python

    buildCommand: pip install -r requirements.txt

    startCommand: python bot.py

    plan: free

    autoDeploy: true

    envVars:
      - key: BOT_TOKEN
        sync: false